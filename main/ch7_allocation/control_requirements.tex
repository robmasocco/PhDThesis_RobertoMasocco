\section{Control requirements}
\label{sec:all_prbs}
As the literature review offered in \Cref{sec:sota_allocation} points out, the current landscape of dynamic control allocation problems and proposed results is quite wide. A dynamic control allocator for the TCV shape controller should be designed to integrate within the existing framework, not only in terms of its implementation but also of the the modeling techniques on which it is based.

For this work, the following formulations of a dynamic control allocation problem have been considered. Their evaluation has led to theoretical results that are illustrated in the following and allowed the author to identify the most suitable approach for the TCV case.

\subsection{Notation}
\label{Sec:notation}
The set $\N$ is defined as containing all the positive integer numbers, excluding $0$.
Given a complex number $z\in\C$, $\Re(z)$ and $\Im(z)$ denote its real and imaginary parts, respectively.
Let $\R_{\geq0}=\{\textbf{a} \in \R:a\geq0\}$, $\Z_{\geq0}=\{\textbf{k} \in \Z:k\geq0\}$, $\C_0=\{\textbf{z} \in \C:\Re(\textbf{z})=0\}$, $\C_{<0}=\{\textbf{z} \in \C:\Re(\textbf{z})<0\}$,	and $\C_{\geq0}=\{\textbf{z} \in \C:\Re(\textbf{z})\geq0\}$.

Given a vector $v\in\R^n$, the symbol $\left\Vert v \right\Vert_2$ denotes its Euclidean norm.
Given a vector space $V$, $\dim(V)$ denotes its dimension.
$e_j$ denotes the $j$-th vector of the canonical basis of	$\R^n$.
The expression $\|v\|^{2}_R$ denotes the product $v'Rv$.

Given a matrix $A$, $\Imag(A)$ denotes its image, $\ker(A)$ denotes its null space, $A'$ denotes its transpose,  $A^{\dagger}$ denotes its Moore-Penrose inverse, and $A^\perp$ denotes a basis matrix for its null space. $\spec(A)$ denotes the spectrum of the matrix $A$.
Given two matrices $A,B\in\R^{m \times n}$ $A \odot B$ denotes their Hadamard, or entrywise, product, while $A \otimes B$ denotes their Kronecker product.
Given the square matrices $\{A_1,\dotsc,A_n\}$, the operator $\diag(A_1,\dotsc,A_n)$ denotes the square matrix obtained by concatenating the former matrices on its diagonal.

The symbol $I_n$ denotes the identity matrix of order $n$, the subscript is omitted when clear from the context.
For a block partitioned matrix $\mathcal{M}$,	$\mathcal{M}_{[k,h]}$ is its $(k,h)-$th block, $\mathcal{M}_{[:,h]}$ is its $h-$th block column, and $\mathcal{M}_{[:,1:h]}$ is the sub-matrix formed by its first $h$ block columns.

$\mathcal{L}_2([a,b])$, $a,b\in\R$, denotes the set of all the functions that are square-integrable over the interval $[a,b]$.

For $T\in\R_{>0}$ and $t\in\R$, the notation $\Mod(t, T)$ indicates the value of $t$ modulo $T$.

\subsection{Preliminary definitions}
\label{sec:defs}
It is instrumental to begin this discussion with a few preliminary definitions and models that are used throughout the chapter. Let $t\in\R_{\geq 0}$ be the time variable. Consider a continuous-time, time-invariant dynamic system $\Sigma$ described by the equations
\begin{equation}
  \label{eq:nlsys}
  \Sigma := \left\{
  \begin{aligned}
    \dot{\chi} & = f(\chi, r) + g(\chi)\upsilon\,, \\
    \eta_p     & = h_p(\chi, \upsilon)\,,       \\
    \eta_o     & = h_o(\chi)\,,
  \end{aligned}
  \right.
\end{equation}
where $\chi(t)\in\R^n$ denotes the state of the system, $r(t)\in\R^{n_r}$ is an exogenous input, $\upsilon(t)\in\R^m$ is an \emph{auxiliary input}, and $\eta_p(t)\in\R^{q_p}$, $\eta_o(t)\in\R^{q_o}$ are outputs. Let one assume that the vector fields $f : R^{n} \times \R^{n_r} \rightarrow \R^n$ and $g_i : \R^{n} \rightarrow \R^{n}$, $i = 1, ..., m$ and the mappings $h_p : \R^{n} \times \R^{m} \rightarrow \R^{q_p}$ and $h_o : \R^{n} \rightarrow \R^{q_o}$ are sufficiently smooth. The system \eqref{eq:nlsys} denotes a \emph{black-box model} with additional input channels that represents many situations: the ones that are of interest in the context of this work are those where plants with a preexisting control system are embedded in a single model of the form of $\Sigma$. These situations are further detailed in the following together with the peculiarity of each case that has been considered. Regarding the outputs, hereby already partitioned in two vectors for convenience, $\eta_p$ is a \emph{performance output} that measures the overall performance of the system $\Sigma$ and $\eta_o$ is a \emph{regulated output}, which captures the desired behavior of the system.

The exogenous input $r(t)$ is periodic of period $T$ and is generated by the Poisson stable (see, \emph{e.g.}, \cite{isidori_nonlinear_1995} for a definition of Poisson stability) \emph{exosystem}
\begin{equation}
  \label{eq:exosys}
  \mathcal{E} := \left\{
  \begin{aligned}
    \dot{w} & = \xi(w)\,,  \\
    r       & = \psi(w)\,,
  \end{aligned}
  \right.
\end{equation}
where $w(t)$ is the exosystem state, $w(t) \in \mathcal{W} \subset \R^{n_e}$, $\mathcal{W}$ compact invariant set, and the vector field $\xi : \R^{n_e} \rightarrow \R^{n_e}$ and the mapping $\psi : \R^{n_e} \rightarrow \R^{n_r}$ are smooth.

The interconnection of the system $\Sigma$ in eq. \eqref{eq:nlsys} with the exosystem $\mathcal{E}$ in eq.\eqref{eq:exosys} is depicted in \Cref{fig:nlsys}. This model is the starting point for the analysis of the dynamic control allocation problems that follows in the rest of this chapter: each of the formulations that have been considered is a specific case that is modeled by \eqref{eq:nlsys} and \eqref{eq:exosys}.
\begin{figure}[t!]
  \centering
  \includesvg[width=.7\textwidth]{images/ch7/nlsys.svg}
  \caption{Block diagram of the system $\Sigma$ in eq. \eqref{eq:nlsys} interconnected to the exosystem $\mathcal{E}$ in eq.\eqref{eq:exosys}.}
  \label{fig:nlsys}
\end{figure}
